services:
  discordctl:
    build: .
    env_file: .env
    environment:
      - AUDIT_PATH=/app/data/audit.jsonl
    network_mode: host
    restart: unless-stopped
    volumes:
      - ./data:/app/data
